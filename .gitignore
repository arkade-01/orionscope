/node_modules
/.next
/out
/build
next-env.d.ts
*.pem
.env*
.DS_Store
*.tsbuildinfo
/design_handoff_orionscope_site
.vercel

# Credential files. `.env*` misses names without a leading dot —
# an env.vercel with a live API key reached a public repo that way.
env*
*.env
